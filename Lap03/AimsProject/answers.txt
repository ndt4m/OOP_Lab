Q1: Try to add a method addDigitalVideoDisc which allows to pass an arbitrary number of arguments for dvd. Compare to an array parameter. What do you prefer in this case?
A1: I like to use Varargs because in case of using array we need to initilize a array then add the desired DVD to that.

Q2: Is JAVA a Pass by Value or a Pass by Reference programming language?
A2: In Java programming language, there's only one technique to pass argument. That is Pass by value.

Q3:After the call of swap(jungleDVD, cinderellaDVD) why does the title of these two objects still remain?
A3: Because the method just only swap the value of the local varible. Therefore the real one is still the same.

Q4: After the call of changeTitle(jungleDVD, cinderellaDVD.getTitle()) why is the title of the JungleDVD changed?
A4: Because dvd is the reference variable of the desired object so it can affect to the real one.

Q5: Write a toString() method for the DigitalVideoDisc class. What should be the return type of this method?
A5: The return type of the method should be String.

